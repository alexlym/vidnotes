Translate the following Markdown study notes into {lang}.

Rules:
- Translate all prose, headings, and list items into {lang}
- Preserve all Markdown formatting exactly (headings, bold, code blocks, bullet points)
- Preserve YAML frontmatter keys verbatim; only translate the values of `title` and `course` if they are human-readable text
- Preserve all code samples, commands, and technical identifiers untranslated
- Do not add commentary, preamble, or explanation — output only the translated document

---

{summary}
